# Проект для запуска Laravel   
Проект на контейнерах docker в котором можно запустить Laravel с базой данных MariaDB (MySQL) 

Laravel нужно развернуть в папке ./laravel иди в любой другой (смотри файл .env) 